---

- name: Install homebrew essentials
  homebrew:
    name: "{{ item }}"
    state: present
  with_items:
    - curl
    - git
    - jq
    - openssl
    - ssh-copy-id
    - tree
    - watch
    - wget
    - zsh
